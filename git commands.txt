1. git status // zeigt den status an, oft hilfreich zu sehen was man als nächstes machen soll


Etwas hinzufügen: 
1. git add * // * = alles
2. git commit -m "commit message" // staged es für den commit 
3. git push origin main // origin = repository von github. main = der Branch
